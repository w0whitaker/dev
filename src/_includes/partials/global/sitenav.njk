{% set currentUrl = page.url %}

<nav>
	<div class="cluster">
		{% for link in siteNavigation %}
			<div class="center">
				<a href="{{ link.target }}" class="box standard-box">
					{{ link.content }}
				</a>
			</div>
		{% endfor %}
	</div>
</nav>
