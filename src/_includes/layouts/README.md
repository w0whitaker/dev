# Layouts.

These files provide the markup for various types of content.

## Base.
The `base.njk` file contains the HTML that is common to all pages: the `<head>`, `<body>`, `<header>`, and `<footer>` elements.

## Page.
The `<main>` element.

## Post.
Markup for blog posts.